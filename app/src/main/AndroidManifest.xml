<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <application
        android:name=".App"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.EndurainBridge">

        <!-- Launcher: settings (server URL + API key). Also the "test upload" entry point. -->
        <activity
            android:name=".ui.SettingsActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Vía B: registers this app as an OpenTracks "dashboard". OpenTracks sends the
             dashboard payload (Track/TrackPoints/Markers content URIs) to this activity.
             Action string confirmed from OpenTracks publicapi/DataProvider.java. -->
        <activity
            android:name=".ui.DashboardReceiverActivity"
            android:exported="true"
            android:excludeFromRecents="true"
            android:noHistory="true"
            android:theme="@style/Theme.EndurainBridge.Transparent">
            <intent-filter>
                <action android:name="Intent.OpenTracks-Dashboard" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <!-- Holds the OpenTracks URI grant during a recording and uploads when it stops (M4). -->
        <service
            android:name=".upload.RecordingWatchService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Waiting for the in-progress OpenTracks recording to finish, then uploading it to Endurain." />
        </service>
    </application>

</manifest>
